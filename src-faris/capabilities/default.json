{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Absen Kelas.",
  "windows": ["main"],
  "permissions": ["opener:default", "process:default", "updater:default"]
}
